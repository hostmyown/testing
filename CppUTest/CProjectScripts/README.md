Script to create a basic C project with CppUTest hooks.

To use this you need the CPPUTEST_HOME set to the home of CppUTest.

The Makefile for the project assumes that the static libraries for CppUTest
are located here:

$CPPUTEST_HOME/lib

